config:
  target: 'http://localhost:8000'
  phases:
    - duration: 10 # 몇 초간
      arrivalRate: 10 # 초당 몇번
scenarios:
  - flow:
      - get:
          url: '/study/experience' # 학습 체험 요청

# back 경로에서 artillery run loadtest.yml